---
title: 객체지향 프로그래밍 여섯, 인터페이스
date: '2021-12-21 00:00:03'
category: Java
draft: false
---

인터페이스는 일종의 추상 클래스인데, 추상화 정도가 더 높아서
오직 추상 메서드와 상수만을 멤버로 가진다.

```java
interface InterfaceName {
  public static final double PI = 3.14;
  public abstract add(int a, int b);
}
```

인터페이스에는 규칙이 존재한다.

- 모든 멤버변수는 `public static final`이어야 하며,
  이를 생략할 수 있다.
- 모든 메서드는 `public abstract`이어야 하며, 이를 생략할 수 있다. (JDK 1.8부터 static 메서드와 디폴트 메서드는 예외)

> 인터페이스는 다중 상속을 지원한다.
> 하지만 다중 상속의 단점이 크다고 보아
> 잘 사용하지 않는 경우가 많다고 한다.

## 인터페이스를 구현

`implements` 키워드를 통해 해당 인터페이스를 구현하는
클래스를 생성할 수 있다. `extends`를 사용하는 것이 아니라.

## 다형성

- 인터페이스를 타입으로서 사용할 수 있다.
- 인터페이스를 구현 클래스의 조상으로 보고,
  인터페이스 타입의 참조 변수로 구현 클래스의 인스턴스를 참조할 수 있다.

## 인터페이스의 장점

- 개발시간을 단축시킬 수 있다.
- 표준화가 가능하다.
- 서로 관계없는 클래스들에게 관계를 맺어줄 수 있다.
- 독립적인 프로그래밍이 가능하다.

## Reference

- 남궁성, Java의 정석 (3rd Edition), 도우출판
